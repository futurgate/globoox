.showcase {
  width: 100%;
  max-width: 1060px;
  margin: 0 auto;
  color: #20382f;
  font-family: var(--ed-sans, Arial, sans-serif);
}

.showcase, .showcase * { box-sizing: border-box; }

.stage {
  display: flex;
  width: 100%;
  height: 624px;
  align-items: center;
  justify-content: center;
}

.frame {
  width: min(960px, 100%);
  flex: 0 1 auto;
  overflow: hidden;
  border: 8px solid #273b32;
  border-radius: 15px;
  background: #f3efe5;
  box-shadow: 0 28px 60px -25px #20382f3d, 0 3px 8px #20382f15;
}

.frame[data-device="tablet"] { width: min(385px, 100%); }
.frame[data-device="phone"] { width: min(250px, 100%); border-radius: 21px; }

.video {
  display: block;
  width: 100%;
  height: auto;
  background: #f3efe5;
  object-fit: contain;
}

.controls {
  display: flex;
  width: fit-content;
  max-width: 100%;
  align-items: center;
  justify-content: center;
  gap: 19px;
  margin: 24px auto 0;
  padding: 5px 6px;
  border: 1px solid #d8decf;
  border-radius: 7px;
  background: #faf9f2e8;
  color: #65705e;
  font-size: 11px;
  line-height: 1.3;
}

.tabs { display: flex; gap: 2px; }

.tabs button {
  display: inline-flex;
  min-height: 40px;
  align-items: center;
  justify-content: center;
  gap: 7px;
  border: 0;
  border-radius: 4px;
  padding: 8px 13px;
  background: transparent;
  color: inherit;
  cursor: pointer;
  font: inherit;
  transition: color 150ms, background 150ms;
}

.tabs button:hover { background: #edf0e5; color: #20382f; }
.tabs button[aria-selected="true"] { background: #20382f; color: #faf8ef; }

.playback {
  display: inline-flex;
  min-width: 78px;
  min-height: 40px;
  align-items: center;
  justify-content: center;
  gap: 7px;
  padding: 8px 10px 8px 14px;
  border: 0;
  border-left: 1px solid #d7ddce;
  border-radius: 0;
  background: transparent;
  color: #52614f;
  cursor: pointer;
  font: inherit;
}

.playback:hover { color: #a16442; }
.controls button:focus-visible { outline: 2px solid #a16c46; outline-offset: 3px; }
.srOnly { position: absolute; width: 1px; height: 1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap; }
.showcase .error { margin: 15px auto 0; color: #895139; text-align: center; font-size: 12px; }

@media (max-width: 600px) {
  .stage { height: 530px; }
  .frame { border-width: 7px; border-radius: 11px; }
  .frame[data-device="tablet"] { width: min(350px, 100%); }
  .frame[data-device="phone"] { width: min(250px, 100%); border-radius: 20px; }
  .controls { gap: 4px; margin-top: 19px; padding: 4px; font-size: 10px; }
  .tabs { gap: 1px; }
  .tabs button { min-height: 44px; gap: 5px; padding: 8px 9px; }
  .tabs svg { width: 14px; }
  .playback { min-width: 65px; min-height: 44px; gap: 5px; padding-inline: 9px; }
}

@media (max-width: 370px) {
  .controls { gap: 3px; }
  .tabs button { padding-inline: 7px; }
  .tabs svg { display: none; }
  .playback { min-width: 63px; }
}

@media (prefers-reduced-motion: reduce) {
  .tabs button { transition: none; }
}
