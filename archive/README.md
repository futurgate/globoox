# Legacy Code Snapshots

This top-level archive contains source-code snapshots of older landing implementations. It is not the documentation archive and none of its files are current application routes.

Current documentation and historical design records live under [`docs/`](../docs/README.md), with historical documents indexed in [`docs/archive/`](../docs/archive/README.md).
